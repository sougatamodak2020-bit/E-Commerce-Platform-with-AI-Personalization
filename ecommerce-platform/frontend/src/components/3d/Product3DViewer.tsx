'use client';
export default function Product3DViewer({ productId }: { productId?: string }) {
  return (
    <div className="w-full h-64 bg-gray-100 rounded-xl flex items-center justify-center">
      <p className="text-gray-400">3D Preview not available</p>
    </div>
  );
}